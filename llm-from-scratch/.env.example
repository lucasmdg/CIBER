# LLM From Scratch - Environment Configuration
# This project does not require any API keys or secrets.
# All operations are local.

# Optional: Override default device (auto-detected if not set)
# DEVICE=cpu
# DEVICE=cuda

# Optional: Data directory override
# DATA_DIR=./data

# Optional: Checkpoint directory override
# CHECKPOINT_DIR=./checkpoints
